name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-20.04
    steps:
      - name: echo a sring
        run: echo "Hello World"
      - name: multiline script
        run: |
          node -v
          npm -v
      - name: ls command
        run: ls -a
